#include <bits/stdc++.h>
using namespace std;



void countFreq(vector<int> *freq, int given) {

}

void waaaa(int &i, vector<int> vector1) {

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int n;
    cin >> n;
    vector<int> arr(n);
    vector<int> freq(n, 0);
    vector<vector<int>>factorMap(n, vector<int>(n));
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
        waaaa(arr[i],arr);
    }

    return 0;
}