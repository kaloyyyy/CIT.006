#include <bits/stdc++.h>
using namespace std;
int arrRecipe[100001];
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int N, Q, R;
    cin >> N >> Q >> R;
    auto start_time = std::chrono::high_resolution_clock::now();
    string temp, reciTemp;
    int value;
    for (int i = 0; i < N; i++) {
        cin >> temp >> value;
        int arr[0] = [value]
        recipe.insert({temp, arr});

    }
}